from setuptools import setup, find_packages
setup(
    name="udntools",
    version="0.0.2",
    packages=["udntools"],
    install_requires=[
        'scipy',
        'numpy',
        'sklearn'
    ]
)
